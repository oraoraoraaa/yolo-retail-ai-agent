[project]
name = "model-roboflow"
version = "0.1.0"
description = "Scripts that connect to the trained models on roboflow."
requires-python = ">=3.13"
dependencies = [
    "inference-gpu>=1.3.5",
    "inference-sdk>=1.3.5",
    "opencv-python>=4.10.0.84",
]

# Core web framework
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# HTTP client for local vision (model-local) and optional LLM calls.
httpx[socks]>=0.27

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23

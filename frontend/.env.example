# Leave empty to use local API stubs (recommended until the backend is ready).
# When set, the frontend will call this origin for audit and chat endpoints.
# Example: http://localhost:8000
VITE_API_BASE_URL=
